[package]
name = "dreamrender"
version = "0.1.2"
description = "DreamRender desktop shell"
authors = ["DreamRender"]
license = "MIT"
repository = "https://github.com/rickgude/DreamRender"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
